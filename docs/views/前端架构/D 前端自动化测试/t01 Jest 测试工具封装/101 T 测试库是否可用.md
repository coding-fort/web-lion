# 测试库是否可用

```js
// tests/debug.test.js
console.log("测试开始：逐步导入依赖");

// 第一步：测试 Axios 是否能正常导入
try {
  require("axios");
  console.log("✅ Axios 导入成功");
} catch (err) {
  console.error("❌ Axios 导入失败:", err);
}

// 第二步：测试 request.js 是否能正常导入
try {
  require("@/utils/request");
  console.log("✅ request.js 导入成功");
} catch (err) {
  console.error("❌ request.js 导入失败:", err);
}

// 第三步：测试路由是否能正常导入
try {
  require("@/router");
  console.log("✅ router 导入成功");
} catch (err) {
  console.error("❌ router 导入失败:", err);
}

// 第四步：测试 Vue 是否能正常导入
try {
  require("vue");
  console.log("✅ Vue 导入成功");
} catch (err) {
  console.error("❌ Vue 导入失败:", err);
}

test("空测试", () => {
  expect(true).toBe(true);
});
```
